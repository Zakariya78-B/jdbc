
import java.io.*;
import java.util.*;

public class exo2 extends Exercise {

	public void runExercise( String[] argv ) throws Exception {
        String frompath = this.getPath()+"/myfile.txt";
        String topath = this.getPath()+"/newfile.txt";
        this.copyFile( frompath, topath );
    }

    public boolean copyFile( String frompath, String topath ) throws Exception {
    	/*----------NE MODIFIEZ PAS LE CODE AU DESSUS DE CETTE LIGNE, IL SERA REINITIALISE LORS DE l'EXECUTION----------*/

         /**** Entrez votre code ici ****/
         
    	/*----------NE MODIFIEZ PAS LE CODE EN DESSOUS DE CETTE LIGNE, IL SERA REINITIALISE LORS DE l'EXECUTION----------*/
    }
}
